# mindmap-mcp-stdio

- **产物**：`mindmap_vscode/mcp-server/dist/index.js`（TypeScript 编译输出）。
- **类型**：Node 脚本，stdio MCP 服务。
- **源码根**：`mindmap_vscode/mcp-server/src/index.ts`。
- **打包**：扩展根目录 `npm run mcp:pack`（`vscode:prepublish` 子步骤）。
